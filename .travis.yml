jobs:
  include:
  - stage: Build

    sudo: required
    services:
    - docker
    script: docker build -t vibioh/domaine:${TRAVIS_COMMIT} .
    after_success: |
      echo ${DOCKER_PASS} | docker login -u vibioh --password-stdin && \
      docker push vibioh/domaine:${TRAVIS_COMMIT}

  - stage: Publish

    sudo: required
    services:
    - docker
    script: |
      echo ${DOCKER_PASS} | docker login -u vibioh --password-stdin
      docker pull vibioh/domaine:${TRAVIS_COMMIT}
      docker tag vibioh/domaine:${TRAVIS_COMMIT} vibioh/domaine:latest
      docker push vibioh/domaine:latest

  - stage: Deploy

    script: skip
    deploy:
      provider: script
      skip_cleanup: true
      script: 'curl -X POST -u "${DEPLOY_CREDENTIALS}" "https://deploy.vibioh.fr/domaine/${TRAVIS_COMMIT}" --data-binary @docker-compose.yml'

stages:
  - Build
  - name: Publish
    if: branch = master
  - name: Deploy
    if: branch = master

notifications:
  email: false
