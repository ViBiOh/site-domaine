---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: domaine
spec:
  selector:
    matchLabels:
      name: domaine
  template:
    metadata:
      labels:
        name: domaine
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/scheme: http
        prometheus.io/port: "1080"
    spec:
      containers:
        - name: domaine
          image: vibioh/domaine
          ports:
            - name: http
              containerPort: 1080
          readinessProbe:
            httpGet:
              path: /health
              port: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
          resources:
            requests:
              memory: 24M
            limits:
              memory: 64M
